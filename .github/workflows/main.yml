name: "vulnerabilty scan"

on:
  pull_request_target:
    types:
      - opened
      - edited
      - synchronize

permissions:
  pull-requests: write

jobs:
  compliance:
    uses: ./.github/workflows/github_actions_dummy.yml@main
